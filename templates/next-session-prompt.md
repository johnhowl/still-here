# 下一轮任务书

> **新会话的开场只需要一句：「读 docs/handover/next-session-prompt.md，按它执行」。**
>
> - 最近更新：{{DATE}} ｜ 对应交接件状态节：**<节号>**
> - **覆盖式，只保留下一轮那一份。** 收口时整段重写，不要追加。
> - **不写会漂移的数字**（测试计数、未推送数、远端位置），跑状态脚本现取。

## 我在哪（每轮收口时重画这棵树）

```
{{PROJECT}}
├─ A. <线> ······· <一句话状态>
├─ B. <线>
│  └─ <子线> ⬅ 当前这条线
│     └─ <阶段> ⬅ 当前在这里
│        ├─ 1 <子任务> ✅
│        └─ 2 <子任务> ⬅ 本轮做这个
└─ C. <线> ······· 🔒 <阻塞原因>
```

**本轮完成后往上走**：

| 回到 | 那一级还剩什么 |
|---|---|
| ↑ <父节点> | |
| ↑↑ <祖父节点> | |
| ↑↑↑ 根 | |

⚠️ 状态在树里画一次，**权威在 tracker**；冲突以 tracker 为准。
树每轮**整体重画**，所以不漂；复制到别处那份副本就会漂。

## 先读

1. `docs/handover/README.md` —— **尤其 §1 的全景**
2. 交接件最后一个状态节
3. tracker 的相关节

## 开工前有没有待拍板的事

<有就用大白话写：现在怎么做的 → 毛病是什么（配具体数字）→ 改了代价是什么>

# 任务：<本轮做什么>

## 判据怎么写

- **每条断言都必须演示它能失败**——撤掉被它保护的那件事，确认变红再改回来。
- **比较用的量先量「无信息基线」**，包括**「这段代码整个不执行时，验收数字是多少」**。
- **差分测量的两端必须指同一件事**，而这是**可测的**，不是可以假定的。

## 现成的东西

| 用途 | 位置 |
|---|---|

## 后面还有什么

## 工作方式

<长期约定，逐轮继承：语言、TDD、禁区、已知偶发失败、提交与推送规矩>

## 收口时怎么更新这个文件

1. 把「先读」到「工作方式」之间整段**重写**成下一轮的任务，不要追加。
2. **重画"我在哪"那棵树。**
3. 更新页首的「最近更新」与「对应交接件状态节」。
4. 检查没有会漂移的数字。
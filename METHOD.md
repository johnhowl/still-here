# 会话交接工作流（可移植版）

**给"由一连串独立 AI 会话推进、每次会话都从零开始"的长期项目用。**
本文自包含，可整份复制到别的项目。§8 列出要替换的东西。

> **它不是最佳实践清单，是踩出来的。** 每条规则后面都有一次真实事故，
> 事故写在规则旁边——**规则会被绕过，事故不会**。

---

## 1. 要解决的问题

AI 会话没有跨会话记忆。项目跑几十轮之后会同时出现三种病：

| 病 | 症状 |
|---|---|
| **失忆** | 新会话不知道做过什么、为什么这么决定，重复劳动或推翻已验证的结论 |
| **文档漂移** | 状态写在多处，只有一处有人维护，其余变成谎言——**而新会话最信任的往往是漂掉的那份** |
| **只见树木** | 新会话只看到"当前那件事"，把"当前任务做完"误当成"项目做完" |

三件套分别对付三件事，**职责不能混**：

| 文件 | 写什么 | 可变性 | 治哪个病 |
|---|---|---|---|
| **任务书** `next-session-prompt.md` | **下一轮**做什么、读什么、别踩什么 | **覆盖式**：每轮整体重写，不追加 | 失忆 |
| **交接件** `<线名>-handover.md` | 每轮追加一个状态节，含**哪些既有结论被订正** | **追加式**，节内可就地订正 | 失忆 + 结论演进 |
| **总账** `tracker.md` | 所有待办的**唯一清单** | 活文档，每完成一项就改 | 漂移（单一真源） |
| **入口页** `README.md` | 全景导航 + 工作流 + 门禁命令 | **只写指针，不写状态** | 只见树木 |

---

## 2. 开场只需要一句

```
读 docs/handover/next-session-prompt.md，按它执行
```

任务书自己指向该读的其余文档。**新会话的第一动作不该是"读一堆文档"，
而是"读一份任务书，它告诉你还要读什么"。**

---

## 3. 任务书骨架（覆盖式，每轮重写）

````markdown
# 下一轮任务书

> - 最近更新：<日期> ｜ 对应交接件状态节：**<节号>**
> - **覆盖式，只保留下一轮那一份。** 收口时整段重写，不要追加。
> - **不写会漂移的数字**（测试计数、未推送数、远端位置），跑 <状态脚本> 现取。

## 我在哪（每轮收口时重画这棵树）

```
<项目名>
├─ A. <线> ······· <一句话状态>
├─ B. <线>
│  └─ <子线> ⬅ 当前这条线
│     └─ <阶段> ⬅ 当前在这里
│        ├─ 1 <子任务> ✅
│        └─ 2 <子任务> ⬅ 本轮做这个
└─ C. <线> ······· 🔒 <阻塞原因>
```

**本轮完成后往上走**：

| 回到 | 那一级还剩什么 |
|---|---|
| ↑ <父节点> | … |
| ↑↑ <祖父节点> | … |
| ↑↑↑ 根 | … |

⚠️ 状态在树里画一次，**权威在总账**；冲突以总账为准。
树每轮**整体重画**，所以不漂；复制到别处那份副本就会漂。

## 先读 / 再跑
1. 入口页（**尤其全景那节**）  2. 交接件最后一个状态节  3. 总账的相关节
```
<状态脚本>
```

## 开工前有没有待拍板的事
<有就用大白话写：现在怎么做的 → 毛病是什么（配具体数字）→ 改了代价是什么>

## 任务：<本轮做什么>
## 判据怎么写
## 现成的东西（表：用途 → 位置）
## 后面还有什么
## 工作方式（长期约定，逐轮继承）
## 收口时怎么更新这个文件
````

**树是这套体系里唯一回答"做完了然后呢"的东西。**
Git 的分支靠 commit 机械地保证能回到上一级；文档里的"回到上一级"
**靠每轮正确重画，是约定不是机制**。所以它必须在覆盖式文件里——
放在追加式文件里，第一轮之后就不准了。

---

## 4. 收口清单（少一件，下一轮就踩空）

1. **重写**任务书的任务段（覆盖，不追加），**重画那棵树**
2. 在交接件**追加**一个新状态节，写清**哪些既有结论被本轮订正了**
3. 在**总账**勾选/更新对应条目
4. **不要**把状态复制进入口页——那是漂移的唯一来源

---

## 5. 两条证据纪律

这两条与领域无关，任何有"判据/测试/验收数字"的项目都适用。

### 5.1 比较用的量，先量它的「无信息基线」

> 凡是要拿来比较、排序或过门限的量，**先算出它在"完全没有信息"时取什么值**，
> 并确认门限在那个值的正确一侧。

无信息基线**不只是**"随机输入下这个量等于多少"，**也包括**：

- **"这段代码整个不执行时，验收数字是多少"**
  （真实事故：某次改动写了三个验收数字，**每一个都能被"函数直接返回 false"满足**，
  于是那段代码从落地起没执行过一次，三轮全绿没人发现）
- **"差分测量的两端是不是同一件事"** —— 而这是**可测的量**，不是可以假定的前提
  （真实事故：差分用了一个每帧可能变的标签，实测那标签只有 62% 的时间成立，
  于是测出来的数字混了两种情形）

### 5.2 新断言必须演示它能失败

> 写完一条新断言，**撤掉被它保护的那件事**，确认它真的变红，再改回来。

⚠️ **手工做只在有人记得时起作用。** 真实事故：同一个病犯了三次，
只有第三次因为有人手工撤掉才发现，前两次都是事后偶然撞见的。

**所以要做成门禁**（形态见 §6），并且：

- **变异要破坏"行为"，不是篡改"断言的输入"**。
  篡改输入证明不了什么；要证的是**「它描述的那件事不再发生时，断言会不会注意到」**。
- **匹配失败信息，不能只看退出码**。否则变异可能被上游别的断言拦下，
  而目标断言仍然不能失败——真实事故：一条变异确实让测试变红了，
  但红在另一条断言上，若只看退出码就会把目标断言错记为"已证明能失败"。
- **不要自动枚举变异**。自动生成的变异多半破坏的是**测量**而不是**行为**，
  "测试注意到测量被破坏了"什么也证明不了。
- **门槛余量只报告，不自动判定**。门槛离实测太远未必是错的——
  有些量物理上就该接近 0，门槛写成"任何可见移动都算问题"是合理的。
  自动判定余量只会造出一个会误报的检查。

### 5.3 元纪律：摘要只留指针，不留值

> 写进摘要前先问：**这句话半个月后还对吗？** 会变的东西只留**指针**，不留**值**。

真实事故：入口页曾有一张"当前整体进度"表和一句"先读第 K 节（当前最新）"。
后来同时烂掉三处——章节指针落后五节、一条状态与配置文件直接矛盾、
一条早已还清的欠账仍被列为"容易被忽略的欠账"。**机制上必然**：
同一件事写两处、只维护一处，另一处就会漂，而漂的那份恰好在入口。

**处置**：入口页的全景节改成**纯导航**（只写"状态去哪看"）；
章节指针改成**自动跟随**（"读最后一个 `## 0x` 节"+ 找它的命令），
而不是写死节号。

---

## 6. 把 5.2 做成门禁（形态，语言无关）

```
测试侧    一个开关（环境变量）选中一个具名变异，测试自己实现它，
          短路掉某个真实行为

驱动侧    一个脚本按表逐条跑：设开关 → 跑测试 →
          要求它变红，且红在**指定的那条断言**上

注册      变成 CI/ctest 的一个用例
```

表里每条要写三件事：**变异名 · 它破坏什么 · 该被哪条断言接住**。

⚠️ **这个检查自己也要演示能失败**：删掉它保护的某条断言，
它必须报"这条变异没被接住"。

---

## 7. 已知失效模式（这套体系自己的）

| 失效 | 表现 | 缓解 |
|---|---|---|
| **覆盖式任务书只承载一条线** | 项目有多条线时，新会话把"任务书做完"当成"项目做完" | 任务书开头画树 + 入口页有全景导航，且任务书指向它 |
| **树靠人重画** | 忘了重画，位置就不准 | 写进收口清单；树在覆盖式文件里，错了下轮修正 |
| 🔴 **收口是手动的，而关掉对话不是** | 用户随时可能直接关窗口。**"读"自动了，"写"没有**——下一轮会照着**上一轮的**任务书去做**已经做完的事**，且没有任何理由怀疑 | **hook 自己查新鲜度**（见 §7b）：报告"任务书最后一次更新之后有几个提交、工作区几处未提交"，>0 就在注入内容前面加过期警告。**不判断有没有收口，只报事实让新会话自己核对** |
| **章节指针依赖命名约定** | "读最后一个 `## 0x` 节"依赖交接件继续这么命名 | 目前无更好办法；换命名时同步改入口页 |
| **交接件越来越长** | 追加式，几十轮后上千行 | 只要求读**最后一节**；更早的靠"以后写的为准"倒读 |
| **摘要复活** | 有人好心又往入口页加状态 | 收口清单第 4 条明确禁止，并留一份归档作反面教材 |

---

## 7b. 让它自动生效（Claude Code）

前面那套靠"新会话记得读任务书"。可以做成**真自动**，两层：

**层 1 — SessionStart hook（真正自动注入）**：`.claude/settings.json`

```json
{
  "hooks": {
    "SessionStart": [{
      "hooks": [{
        "type": "command",
        "command": "f=\"${CLAUDE_PROJECT_DIR:-.}/docs/handover/next-session-prompt.md\"; [ -f \"$f\" ] && jq -Rs '{hookSpecificOutput:{hookEventName:\"SessionStart\",additionalContext:.}}' \"$f\" || true",
        "timeout": 10
      }]
    }]
  }
}
```

任务书内容在会话开场就已在上下文里，**开新对话一句话都不用说**。
`|| true` 与 `-f` 检查是必须的：任务书缺失时 hook 必须静默，不能让会话起不来。

⚠️ **hook body 要写成脚本而不是一行命令**，因为它还得干第二件事——**查新鲜度**：

```
任务书最后一次被改的提交 → 此后还有几个提交？工作区几处未提交？
两者任一 > 0  →  在注入内容前面加一段过期警告
```

**这是必须的**：加载自动了，**收口没有**。用户随时可能直接关掉对话，
那么下一轮读到的是**上一轮的**任务书，会去做**已经做完的事**，而且它没有理由怀疑。

**只报事实，不下判断**——"之后有 N 个提交、M 处未提交"，让新会话自己去核对
（跑状态脚本、`git log`、看交接件最后一节）。收口后提交的那一轮，N=0，不打扰。
误报是可以接受的（收口后又改了个 typo），**漏报不行**。

**层 2 — `CLAUDE.md`（兜底 + 触发词）**：hook 可能被禁用或换客户端，
所以 `CLAUDE.md` 里也写一条"自己去读那份任务书"。
同时在这里定义**收口触发词**——用户说一句
「按交接工作流收口」就执行 §4 的四件事，不用每次复述。

⚠️ **两层都要**：hook 保证自动，`CLAUDE.md` 保证 hook 失效时不静默降级成失忆。

## 8. 移植到新项目要改什么

| 要改 | 说明 |
|---|---|
| 文档根路径 | 本文假设 `docs/{design,plan,testing,handover}/`，可改 |
| **状态脚本** | 一个打印"分支/工作区/未推送数/测试计数"的脚本，任务书靠它避免写死数字 |
| **门禁命令** | 构建与测试怎么跑、哪些必须全绿 |
| **树的五条线** | 换成本项目的实际分线 |
| **交接件命名** | `<线名>-handover-<日期>.md`，状态节用 `## 0a/0b/0c…` 递增 |
| §5 的两条纪律 | **建议原样保留**，它们与领域无关 |

**不要改的**：三件套的职责分工、收口清单、"摘要只留指针"这条元纪律。
它们是这套体系能自我纠错的原因。

---

## 9. 一页速查

```
开新会话 → 「读 <任务书路径>，按它执行」

任务书  = 下一轮做什么（覆盖式，含"我在哪"的树）
交接件  = 每轮追加状态节（含订正了什么）
总账    = 所有待办的唯一清单（冲突以它为准）
入口页  = 全景导航（只写指针，不写状态）

收口四件：重写任务书+重画树 · 追加交接件状态节 · 更新总账 · 别往入口页写状态

判据两条：先量无信息基线（含"代码不执行时"）· 新断言必须演示能失败（做成门禁）
```
